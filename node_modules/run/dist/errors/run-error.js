const runErrorSymbol = /* @__PURE__ */ Symbol.for("vercel.run.error.RunError");
class RunError extends Error {
  [runErrorSymbol] = true;
  /** Stable machine-readable error code. */
  code;
  /** Optional structured diagnostic details. */
  details;
  constructor(message, code = "RUN_ERROR", details) {
    super(message);
    this.name = new.target.name;
    this.code = code;
    this.details = details;
  }
  /** Identifies `RunError` instances across duplicate package copies. */
  static isInstance(error) {
    return error !== null && typeof error === "object" && runErrorSymbol in error && error[runErrorSymbol] === true;
  }
}
export {
  RunError
};
//# sourceMappingURL=run-error.js.map