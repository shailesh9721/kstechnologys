import { RunError } from "./run-error.js";
class RunTimeoutError extends RunError {
  constructor(timeoutMs) {
    super(
      `JavaScript runtime execution timed out after ${timeoutMs}ms.`,
      "RUN_TIMEOUT",
      { timeoutMs }
    );
  }
}
export {
  RunTimeoutError
};
//# sourceMappingURL=run-timeout-error.js.map