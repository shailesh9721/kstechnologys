import { Buffer } from "node:buffer";
import { RunProtocolError } from "../errors.js";
const invalidMessage = (kind) => new RunProtocolError(`Invalid ${kind} worker protocol message.`);
const isRecord = (value) => typeof value === "object" && value !== null && !Array.isArray(value);
const isTimestamp = (value) => Number.isSafeInteger(value) && value >= 0;
const isIdentifier = (value) => typeof value === "string" && value.length > 0 && value.length <= 512;
const assertExactKeys = (value, expected) => {
  const actual = Object.keys(value).toSorted();
  const sortedExpected = expected.toSorted();
  if (actual.length !== sortedExpected.length || actual.some((key, index) => key !== sortedExpected[index])) {
    throw invalidMessage("object shape");
  }
};
const isSerializableError = (value) => {
  if (!isRecord(value)) {
    return false;
  }
  const allowed = /* @__PURE__ */ new Set(["code", "details", "message", "name", "stack"]);
  if (Object.keys(value).some((key) => !allowed.has(key))) {
    return false;
  }
  return typeof value.name === "string" && typeof value.message === "string" && (value.stack === void 0 || typeof value.stack === "string") && (value.code === void 0 || typeof value.code === "string");
};
const isRunOptions = (value) => {
  if (!isRecord(value)) {
    return false;
  }
  try {
    assertExactKeys(value, [
      "executionTimeoutMs",
      "maxConsoleOutputBytes",
      "maxHostFunctionInputBytes",
      "maxResultBytes",
      "maxStackSizeBytes",
      "memoryLimitBytes",
      "timeoutMs"
    ]);
  } catch {
    return false;
  }
  return Object.values(value).every(
    (item) => Number.isSafeInteger(item) && item > 0
  );
};
const isDeterminism = (value) => {
  if (!isRecord(value)) {
    return false;
  }
  try {
    assertExactKeys(value, ["dateNowMs", "randomSeed"]);
  } catch {
    return false;
  }
  return isTimestamp(value.dateNowMs) && typeof value.randomSeed === "string" && /^[0-9a-f]{32}$/u.test(value.randomSeed);
};
const assertReady = (value) => {
  assertExactKeys(value, ["invocationId", "type"]);
  if (!isIdentifier(value.invocationId)) {
    throw invalidMessage("ready");
  }
};
const assertResult = (value) => {
  if (value.success === true) {
    assertExactKeys(value, ["invocationId", "success", "type", "valueJson"]);
    if (!isIdentifier(value.invocationId) || typeof value.valueJson !== "string" || value.valueJson.length === 0) {
      throw invalidMessage("result");
    }
    return;
  }
  assertExactKeys(value, ["error", "invocationId", "success", "type"]);
  if (value.success !== false || !isIdentifier(value.invocationId) || !isSerializableError(value.error)) {
    throw invalidMessage("result");
  }
};
const assertBridgeIdle = (value) => {
  assertExactKeys(value, [
    "invocationId",
    "requestCount",
    "responseCount",
    "type"
  ]);
  if (!isIdentifier(value.invocationId) || !Number.isSafeInteger(value.requestCount) || value.requestCount < 0 || !Number.isSafeInteger(value.responseCount) || value.responseCount < 0 || value.responseCount > value.requestCount) {
    throw invalidMessage("bridge-idle");
  }
};
const assertHostFunctionRequest = (value) => {
  assertExactKeys(value, [
    "hostFunctionName",
    "inputJson",
    "invocationId",
    "requestId",
    "requestIndex",
    "type"
  ]);
  if (!isIdentifier(value.invocationId) || !isIdentifier(value.requestId) || !Number.isSafeInteger(value.requestIndex) || value.requestIndex <= 0 || typeof value.hostFunctionName !== "string" || value.hostFunctionName.length === 0 || Buffer.byteLength(value.hostFunctionName) > 1024 || typeof value.inputJson !== "string" || value.inputJson.length === 0) {
    throw invalidMessage("host-function-request");
  }
};
const assertBridgeResponse = (value) => {
  const common = isIdentifier(value.invocationId) && isIdentifier(value.requestId) && typeof value.success === "boolean" && isTimestamp(value.dateNowMs);
  if (value.success === true) {
    assertExactKeys(value, [
      "dateNowMs",
      "invocationId",
      "requestId",
      "success",
      "type",
      "valueJson"
    ]);
    if (!common || typeof value.valueJson !== "string" || value.valueJson.length === 0) {
      throw invalidMessage("bridge-response");
    }
    return;
  }
  assertExactKeys(value, [
    "dateNowMs",
    "error",
    "invocationId",
    "requestId",
    "success",
    "type"
  ]);
  if (!common || !isSerializableError(value.error)) {
    throw invalidMessage("bridge-response");
  }
};
const hasValidSyncBridgeConfiguration = (value) => (value.syncBridge === void 0 || value.syncBridge instanceof SharedArrayBuffer) && (!value.moduleLoader || value.syncBridge !== void 0) && (!Array.isArray(value.syncHostFunctionNamespaces) || value.syncHostFunctionNamespaces.length === 0 || value.syncBridge !== void 0);
const hasValidSourceType = (value) => value === "function-body" || value === "module";
const assertMainToWorkerMessage = (value) => {
  if (!isRecord(value) || typeof value.type !== "string") {
    throw invalidMessage("main-to-worker");
  }
  if (value.type === "run") {
    assertExactKeys(value, [
      "determinism",
      "hostFunctionNamespaces",
      "invocationId",
      "moduleLoader",
      "options",
      "source",
      "sourceType",
      "syncBridge",
      "syncHostFunctionNamespaces",
      "type"
    ]);
    if (!isIdentifier(value.invocationId) || typeof value.source !== "string" || !Array.isArray(value.hostFunctionNamespaces) || value.hostFunctionNamespaces.some((item) => !isIdentifier(item)) || new Set(value.hostFunctionNamespaces).size !== value.hostFunctionNamespaces.length || !Array.isArray(value.syncHostFunctionNamespaces) || value.syncHostFunctionNamespaces.some((item) => !isIdentifier(item)) || new Set(value.syncHostFunctionNamespaces).size !== value.syncHostFunctionNamespaces.length || typeof value.moduleLoader !== "boolean" || !hasValidSourceType(value.sourceType) || !hasValidSyncBridgeConfiguration(value) || !isDeterminism(value.determinism) || !isRunOptions(value.options)) {
      throw invalidMessage("run");
    }
    return;
  }
  if (value.type === "bridge-response") {
    assertBridgeResponse(value);
    return;
  }
  if (value.type === "cancel") {
    assertExactKeys(value, ["invocationId", "type"]);
    if (!isIdentifier(value.invocationId)) {
      throw invalidMessage("cancel");
    }
    return;
  }
  throw invalidMessage("main-to-worker");
};
const assertWorkerToMainMessage = (value) => {
  if (!isRecord(value) || typeof value.type !== "string") {
    throw invalidMessage("worker-to-main");
  }
  if (value.type === "host-function-request") {
    assertHostFunctionRequest(value);
    return;
  }
  if (value.type === "bridge-idle") {
    assertBridgeIdle(value);
    return;
  }
  if (value.type === "result") {
    assertResult(value);
    return;
  }
  if (value.type === "ready") {
    assertReady(value);
    return;
  }
  throw invalidMessage("worker-to-main");
};
export {
  assertMainToWorkerMessage,
  assertWorkerToMainMessage
};
//# sourceMappingURL=protocol-validation.js.map